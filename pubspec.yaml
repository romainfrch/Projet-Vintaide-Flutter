name: vintaide
description: Application Vintaide

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  web: ^1.1.0
  flutter:
    sdk: flutter

  firebase_core: ^2.27.0
  cloud_firestore: ^4.15.0
  google_fonts: ^6.2.1

  cupertino_icons: ^1.0.6
  file_picker: ^10.3.10
  image: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
